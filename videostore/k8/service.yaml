apiVersion: v1
kind: Service
metadata:
  name: videostore-service
  labels: { app: videostore }
spec:
  type: NodePort
  selector:
    app: videostore
  ports:
    - name: http
      port: 80
      targetPort: 8000   # must match containerPort above


